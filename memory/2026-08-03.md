# Forge 项目日志 · 2026-08-03

## W1 唤醒计划启动（D1 材料就绪）

**背景：** 待办里 W1 一直挂起，今天（周日）启动。Wokwi 已验证可访问（200 OK），纯浏览器零硬件。

**方案：** 在 Forge 新建 `w1/` 目录，按天组织（D1-D7 骨架），先交付 D1 完整材料。

**产出（commit 6b42846 已推）：**
- `w1/README.md` — W1 总览 + 任务表（验证标准对齐 4week-awakening.md）+ 每日节奏 + 产出约定
- `w1/D1-blink/sketch.ino` — ESP32 LED 闪烁（GPIO2 + 220Ω + LED，带教学注释：pinMode/digitalWrite/delay/millis 概念 + 串口调试习惯）
- `w1/D1-blink/diagram.json` — Wokwi 电路图（esp32-devkit-v1 + resistor + led）
- `w1/D1-blink/README.md` — 10 分钟操作步骤 + 概念速记表 + 4 道思考题（含为 D3 PWM 预热的 GPIO4 题）

**流程：** 提交前 ocr review → 0 findings（.ino 被识别审查，md/json 自动排除）→ 提交推送。

**待用户动作：** 浏览器跑 D1（Wokwi 粘贴 sketch.ino + diagram.json → ▶ 运行），完成后回填 w1/README.md 任务表状态。

**下一步：** D2 按键中断 + 防抖（等用户 D1 完成信号，或用户主动要求时准备）。

## 夜间追加：W1 训练包 D2-D7 全量交付（优化版方案落地）

- **方案演进**：用户批准优化版（方案 1 + 7 项优化）→ 一次备齐 W1 全部材料
- **质量门禁**：新建 `scripts/wokwi-lint/`（官方 @wokwi/diagram-lint 封装，node lint.js <diagram>）——**已抓修 3 处引脚错误**：esp:2→esp:D2、按钮 1/2→1.l/2.l、逻辑分析仪 0→D0。所有 D 交付前 0 error 验证
- **关键发现：Wokwi 没有 BME280 部件**（137 部件注册表实测）→ D5 改用 **MPU6050**（I2C 六轴 IMU，地址 0x68，仿真可交互改读数），I2C 教学点完全一致
- **D4 方案**：双 ESP32 互联不确定 → 用 **loopback 自发自收**（TX2↔RX2 短接，UART 调试第一课）+ 字符串帧玩法
- **D6 主线串讲**：5 个 demo = 智能灯具雏形（输入/大脑/输出/通信四角色），对照真实照明系统映射 + 中断原理完整讲解 + 15 题自测
- **D7 复盘**：W1→W2 能力映射表 + W2 前置清单（com0com→socat 问题已标注）
- **ocr review**：抓出 ESM require 兼容性 bug（@wokwi/diagram-lint 是纯 ESM）→ 改动态 import()，修复后 0 findings
- **提交**：1a5b4ab 已推（D2-D7 全量 + lint 工具）
- **踩坑**：Node 22 严格模式——顶层 require + top-level await 混用报 ERR_AMBIGUOUS_MODULE_SYNTAX，动态 import 必须放 async 函数内
